"""Token-efficiency and indexing benchmarks.

Measurements are recorded, never claimed: the token benchmark compares raw
source context vs graph-only context vs graph + targeted source context using
a conservative ``chars / 4`` token estimate.
"""
